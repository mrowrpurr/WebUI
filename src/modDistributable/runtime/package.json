{
  "name": "webui-private-runtime",
  "version": "1.0.0",
  "description": "Skyrim Platform Runtime for WebUI",
  "author": "Mrowr Purr <mrowr.purr@gmail.com>",
  "license": "MIT",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "setup": "node ./scripts/setup.js",
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "deploy": "node ./scripts/deploy.js",
    "build:deploy": "npm run build && npm run deploy",
    "build:watch": " tsc-watch --onSuccess \"node ./scripts/deploy.js\""
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "glob": "^7.2.0",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "tsc": "^2.0.3",
    "tsc-watch": "^4.5.0",
    "typescript": "^4.5.2"
  }
}
